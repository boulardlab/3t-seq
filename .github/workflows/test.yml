name: Run tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  # run-unit-tests:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #       with:
  #         submodules: recursive
  #         lfs: true

  #     - name: Setup Pixi
  #       uses: prefix-dev/setup-pixi@v0.8.1
  #       with:
  #         pixi-version: v0.39.1
  #         cache: true

  #     - name: Run Pytest
  #       run: pixi run unit-test

  run-integration-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          lfs: true

      - name: Setup Pixi
        uses: prefix-dev/setup-pixi@v0.8.1
        with:
          pixi-version: v0.66.0
          cache: true
          environments: dev

      - name: Run Integration Tests
        run: |
          ROOT=$(realpath .)
          pixi run -e dev test adaptive-trim laptop \
          --singularity-args="--bind $ROOT --bind $HOME"
